<form method="post" action="{{ path('app_utilisateur_delete', {'id': utilisateur.idUtilisateur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.idUtilisateur) }}">
    <button class="btn">Supprimer le compte</button>
</form>
